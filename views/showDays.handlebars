<form action="/showScheduledDays/{{name}}" method="POST">
	<div>


		<h3>Hi {{name}}, below is the schedule for available waiters:</h3> <br>

	
	{{!-- <input type="text" name="days">{{getDay}}</div> --}}
		<table class="table table-striped table-warning table-hover table-responsive-md w-auto">

		

			{{#each getDays}}
			<th class="{{color}}" value= "{{id}}" name = "day_id">{{day}}</th>
			{{/each}}
<tr>
		<td class="bg_table">

				<p>{{#each monday}}
				<a href="/schedule/{{name}}">{{name}} </a><br>
				{{/each}}</p><br>
				<a class="btn btn-danger" href="/resetDay/Monday" type="" name="">Reschedule</a><br>
		
			</td>

			<td>
				<p>
				{{#each tuesday}}
				<a href="/schedule/{{name}}">{{name}}</a><br>
				{{/each}}</p><br>
				<a class="btn btn-danger" href="/resetDay/Tuesday" type="button" name="day_id">Reschedule</a>
			</td>
			<td>
				<p>{{#each wednesday}}
				<a href="/schedule/{{name}}">{{name}}</a> <br>
				{{/each}}</p><br>
				<a class="btn btn-danger" href="/resetDay/Wednesday" type="" name="">Reschedule</a></span>
			</td>
			<td>
				<p>{{#each thursday}}
				<a href="/schedule/{{name}}">{{name}}</a> <br>
				{{/each}}</p><br>
				<a class="btn btn-danger" href="/resetDay/Thursday" type="" name="">Reschedule</a></span>
			</td>
			<td>
				<p>{{#each friday}}
				<a href="/schedule/{{name}}">{{name}}</a> <br>
				{{/each}}</p><br>
				<a class="btn btn-danger" href="/resetDay/Friday" type="" name="">Reschedule</a></span>
			</td>
			<td>
				<p>{{#each saturday}}
				<a href="/schedule/{{name}}">{{name}}</a> <br>
				{{/each}}</p><br>
				<a class="btn btn-danger" href="/resetDay/Saturday" type="" name="">Reschedule</a><br>
				
			</td>
			<td>
				<p>{{#each sunday}}
				<a href="/schedule/{{name}}">{{name}}</a> <br>
				{{/each}}</p><br>
				<a class="btn btn-danger" href="/resetDay/Sunday" type="" name="">Reschedule</a><br>
			</td>
</tr>

		
		


	 <div><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="green" class="bi bi-circle-fill" viewBox="0 0 16 16">
  <circle cx="8" cy="8" r="8"/>
</svg><p>Enough waiters </p></div><br>
	 <div ><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="red" class="bi bi-circle-fill" viewBox="0 0 16 16">
  <circle cx="8" cy="8" r="8"/>
</svg><p>Overbooked</p></div><br>
	 <div><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="orange" class="bi bi-circle-fill" viewBox="0 0 16 16">
  <circle cx="8" cy="8" r="8"/>
</svg><p>Waiters needed</p></div>
	</div>
</form>